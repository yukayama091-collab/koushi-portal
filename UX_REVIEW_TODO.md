# 講師ポータル UX/UI レビュー — 改善TODO

**作成日**: 2026-07-05
**対象バージョン**: v2026.07.03-b

---

## 🔴 Critical（必ず直す）

### 1. 初回ロードが 14〜45 秒かかる
- 原因: GAS Web App が 2.4MB の JSON を毎回全走査
- 改善案:
  - progressOld/progressNew/dataOrder は毎回全件不要 → 差分同期に切替
  - 直近更新の生徒だけ差分取得する `since` パラメータ導入
  - 初回はキャッシュから即表示 → バックグラウンドで再取得
- 効果: 30秒以上 → 2秒以内

### 2. エラーメッセージが技術者向け英字で読解不能
- 例: `Failed to fetch`, `forbidden`, `invalid token`, `無効な引数: https://graph.facebook.com...`
- 改善案: エラーコード → 日本語文言テーブル
  - `Failed to fetch` → 「通信に失敗しました。数秒待って画面右上の『更新』を押してください」
  - `forbidden` → 「この操作の権限がありません。管理者に依頼してください」

### 3. モーダル/ダイアログ体系が 6 種類混在
- ネイティブ confirm() / alert() / iosConfirm/iosAlert / .overlay .modal / ドロワー中央モーダル / ヘッドアップUI
- 改善案: 全て iOS風モーダル(iosConfirm/iosAlert) に統一

### 4. サイドバーに「今後」と書かれた押せない項目
- 「従業員・MG (今後)」「クラス (今後)」
- 改善案: リリースまで完全に隠す

### 5. 保存に「Undo」がない
- 講師編集の「保存する」はスプシに直書き込み
- 改善案: 保存後にスナックバー「保存しました [元に戻す]」10秒表示 + `undo_last_edit` action追加

### 6. 生徒詳細で「講師データは講師フィルタ選択時に取得されます」の注釈が本文に混在
- 改善案: 自動取得は実装済み。スケルトンで待たせて取得完了後に置換。文言削除。

---

## 🟠 High（できれば直す）

1. **サイドバー「株式会社Levela」がテキスト** → ロゴSVG化
2. **ドーナツチャート6個並列**は合計100%にならず混乱 → 積み上げ横棒 or 単一ドーナツ+凡例
3. **列フィルタの操作方法が2つ混在**（クリック/ホバー） → クリックで開閉のみに統一
4. **iPad/スマホで検索/ドロップダウン/チップが5段になる** → 480px以下は集約
5. **「更新」ボタンが目立たない** → ステート付きボタン + 最終更新時刻表示
6. **ロール別の権限説明が画面上にない** → ロールバッジクリックで「あなたにできること」パネル
7. **検索バーの inline SVG と top-filter の高さがピクセル単位でズレる** → 38px+line-height揃え徹底
8. **ユーザー管理でURL列が長すぎ横スクロール必要** → コピーボタンのみ、URL本体は詳細で

---

## 🟡 Medium

- KPI 4枚のフォントウェイト階層が浅い
- チップの件数バッジのパディング不均等
- バージョン表記 (v2026.07.03-b) は管理者以外に非表示
- 「担当生徒は生徒マスタと連携して...」の説明文が前提知識を求めすぎ
- iOS風モーダルのタイトル文字サイズが本文と近すぎヒエラルキー弱い
- system-ui → Inter+Noto Sans JP に切替で SaaS感UP
- 今日のフォロー、経過日数2桁3桁でバッジがずれる
- 全講師の状況カードのホバーアニメがない

---

## 🔵 Low

- ✓ (U+2713) が emoji 削除後も残存
- URL入力欄の placeholder が40字強と長い
- 日付「2026年6月30日(月)」の曜日カッコがJIS全角
- サイドバー項目のスペース有無が不統一
- console.log が本番に残存

---

## ⑥ 予想バグ・危険な仕様

1. **IG Access Token 期限切れ** — Facebook トークンは60日で切れる。監視/通知なし。
2. **update_teacher 楽観ロックなし** — 同時保存で片方の変更が消える
3. **iosConfirm 中のリロードで in-memory 状態残存**
4. **DEMO_TOKEN が Code.gs に平文ハードコード**
5. **GAS の doPost が 302 → 405 になるケース** — issue_token の Failed to fetch が偶発的に再発
6. **画面遷移時にモーダルが閉じない** — 講師ドロワー開いたまま go() で操作不能ゾーン

---

## 最終評価

| 項目 | 点数 (100点満点) |
|---|---|
| UI | 55 |
| UX | 50 |
| デザイン | 58 |
| 機能 | 72 |
| 内容 | 60 |
| 保守性 | 30 |

**Apple/Linear を100点とした場合: 48点**

### 一番致命的な問題
初回ロード時間が30秒近くかかる。これを直さないと土台ごと崩れる。

### 最も費用対効果の高い改善3つ
1. **loadData のインクリメンタル化**（差分同期）— 30秒 → 3秒
2. **モーダル体系を iOS風モーダル1種に統一**
3. **サイドバー「今後」項目の削除 + ロゴSVG化**

---

## 実施順序の推奨

1. Critical#4 (サイドバー今後削除 + ロゴ) — 30分, 素人臭が消える
2. Critical#2 (エラー日本語化) — 1〜2時間, サポート問合せ半減
3. Critical#3 (モーダル統一) — 2〜3時間, プロっぽさUP
4. Critical#5 (保存Undo) — 半日, 誤操作リスク激減
5. Critical#1 (loadData差分同期) — 1〜2日, 最大の体感改善
6. Critical#6 (生徒詳細の講義データ表示) — 1時間
7. High 順に対応
