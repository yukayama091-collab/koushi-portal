# 講師ポータル 反映依頼（2026-08-10）

`https://app.levela.co.jp/instructor-dashboard/` で配信している講師ポータルの HTML を、
最新版に差し替えていただきたいです。

**誤表示の修正を含むため、優先して反映していただけると助かります。**

## 依頼内容

- **差し替えるファイル**: リポジトリ `yukayama091-collab/koushi-portal`（public）の `koushi_portal.html`
- **反映するコミット**: `27b454c`（main の最新）
- **配置先**: 現在 `https://app.levela.co.jp/instructor-dashboard/` で配信しているファイル

```bash
git clone https://github.com/yukayama091-collab/koushi-portal.git
# 既存クローンがあれば
git pull

# 反映前にバックアップ（配置先は実際のパスに合わせてください）
cp /var/www/.../instructor-dashboard/index.html \
   /var/www/.../instructor-dashboard/index.html.20260810.bak

# 差し替え
cp koushi-portal/koushi_portal.html /var/www/.../instructor-dashboard/index.html
```

## 変更の性質

- **単一の静的 HTML ファイルの差し替えのみ**です
- ビルド作業はありません（依存パッケージなし、単一ファイル完結）
- サーバー設定・nginx 設定・証明書・DNS の変更は不要です
- バックエンド（GAS 受付窓口）側は対応済みで、**先に配置しても問題ありません**

## 現在配信中の版

配信中のファイルはコミット `386c057`（2026-08-05）の内容と一致していました。
今回は **4コミット**がまとめて反映されます。

## 反映される内容

### 1. 講師マスタで担当MGが別人の名前で表示される問題の修正（最優先）

講師マスタ → ローンチタブの「講師MG」「サポMG」がプルダウンですが、選択肢が
コードに直書き（サポMGは6個のみ）されていました。HTML の `<select>` は実際の値が
選択肢に無いと**先頭の項目を表示する**ため、リストに載っていない担当者が全員
先頭の名前として表示されていました。

- サポMG: 値がある216件のうち **147件** が実際とは違う「kenta」と表示
- 講師MG: 値がある378件のうち **238件** が実際とは違う「こうし」と表示

例として講師ID 0062（みき）は、正しくは 講師MG=はる／サポMG=やんけ ですが、
画面では こうし／kenta と表示されていました。

**データ自体は正しく、表示だけの問題**です。修正内容は次のとおりです。

- 選択肢を講師DBの「講師MGリスト」「サポMGリスト」から読み込む（コードに名前を持たない）
- 「未確定」「無し」を選択肢に追加
- 一覧に無い値は選択肢の先頭に加えて、誤表示が起きないようにする

### 2. ユーザー管理の新規発行にMGの登録区分を追加

講師MG・サポートMGを選んだときに「既存講師から登録／新規採用MG」を選べるようにしました。
新規を選ぶと記入式（MG名・アカウント名・アカウントリンク）に切り替わり、
登録時に講師IDが自動採番されます（講師MG 2000〜／サポートMG 3000〜）。

## 反映後の確認方法

1. `https://app.levela.co.jp/instructor-dashboard/` をブラウザで開く（**強制リロード**: Mac は Cmd+Shift+R）
2. 講師マスタ → 講師ID **0062（みき）** を開く → ローンチタブ
3. 講師MG が **はる**、サポMG が **やんけ** と表示されていれば反映成功
   （反映前は こうし／kenta と表示されます）

## 切り戻し

上記バックアップを書き戻すだけで元に戻ります。データベースやスプレッドシートには一切影響しません。

## 連絡先

不明点があれば 山下優花（CS/CTO室）まで。
