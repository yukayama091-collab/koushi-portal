# 講師ポータル 反映依頼（2026-08-05）

`https://app.levela.co.jp/instructor-dashboard/` で配信している講師ポータルの HTML を、
最新版に差し替えていただきたいです。

## 依頼内容

- **差し替えるファイル**: リポジトリ `yukayama091-collab/koushi-portal`（public）の `koushi_portal.html`
- **反映するコミット**: `20bd194`（main の最新）
- **配置先**: 現在 `https://app.levela.co.jp/instructor-dashboard/` で配信しているファイル

```bash
git clone https://github.com/yukayama091-collab/koushi-portal.git
# 既存クローンがあれば
git pull

# 反映前にバックアップ（配置先は実際のパスに合わせてください）
cp /var/www/.../instructor-dashboard/index.html \
   /var/www/.../instructor-dashboard/index.html.20260805.bak

# 差し替え
cp koushi-portal/koushi_portal.html /var/www/.../instructor-dashboard/index.html
```

## 変更の性質

- **単一の静的 HTML ファイルの差し替えのみ**です
- ビルド作業はありません（依存パッケージなし、単一ファイル完結）
- サーバー設定・nginx 設定・証明書・DNS の変更は不要です
- バックエンド（GAS 受付窓口）側は対応済みで、**先に配置しても問題ありません**

## 現在配信中の版

配信中のファイルはコミット `fff26c4`（サイドバーのロゴ修正）の内容と一致していました。
今回反映されるのは **1コミットのみ**です。

## 反映される内容

### whoami 失敗時に権限判定が抜ける問題の修正（`20bd194`）

ログイン中のユーザー情報を取得する `whoami` が1回でも失敗すると、権限判定の処理が
実行されないまま `role=unknown` で固定されていました。

「該当ロールのときだけ隠す」作りだったため、**判定できないと管理系メニューが
すべて表示されてしまう**状態でした。具体的には、講師ロールの利用者に
「講師マスタ」「ユーザー管理」「編集履歴」が見えてしまいます。
サイドバー下部も「確認中…」のまま止まります。

GAS は一時的に HTML のエラーページを返すことがあり（実測で 5% 程度）、実際に起こりえます。

修正内容:

- `whoami` を最大3回リトライする（応答が JSON でない場合も失敗として扱う）
- 判定できないうちは**隠す**方向に統一。起動時点でも、権限が確定するまで管理系メニューを非表示にする
- 最終的に失敗したら「権限を確認できません／通信に失敗しました」と表示し、再試行ボタンを出す

**データが漏れる不具合ではありません。** メニューが見えるだけで、実際の読み書きは
GAS 側がトークンのロールで判定して拒否します。表示上の問題の修正です。

## 反映後の確認方法

1. `https://app.levela.co.jp/instructor-dashboard/` をブラウザで開く（**強制リロード**: Mac は Cmd+Shift+R）
2. サイドバー下部に、ログイン中のアカウント名とロールが表示されている（「確認中…」のままにならない）
3. 管理者アカウントでは「講師マスタ」「ユーザー管理」「編集履歴」が表示される
4. 講師アカウントでは上記3つが表示されない

## 切り戻し

上記バックアップを書き戻すだけで元に戻ります。データベースやスプレッドシートには一切影響しません。

## 連絡先

不明点があれば 山下優花（CS/CTO室）まで。
