# 講師ポータル 反映依頼（2026-08-04）

`https://app.levela.co.jp/instructor-dashboard/` で配信している講師ポータルの HTML を、
最新版に差し替えていただきたいです。

## 依頼内容

- **差し替えるファイル**: リポジトリ `yukayama091-collab/koushi-portal`（public）の `koushi_portal.html`
- **反映するコミット**: `27d6084`（main の最新）
- **配置先**: 現在 `https://app.levela.co.jp/instructor-dashboard/` で配信しているファイル

リポジトリ側のファイル名は `koushi_portal.html`、配信側はディレクトリURLなので `index.html`
かと思います。**中身をコピーするだけ**で、ファイル名は現在配信中のものに合わせてください。

```bash
git clone https://github.com/yukayama091-collab/koushi-portal.git
# 既存クローンがあれば
git pull

# 反映前にバックアップ（配置先は実際のパスに合わせてください）
cp /var/www/.../instructor-dashboard/index.html \
   /var/www/.../instructor-dashboard/index.html.20260804.bak

# 差し替え
cp koushi-portal/koushi_portal.html /var/www/.../instructor-dashboard/index.html
```

## 変更の性質

- **単一の静的 HTML ファイルの差し替えのみ**です
- ビルド作業はありません（依存パッケージなし、単一ファイル完結）
- サーバー設定・nginx 設定・証明書・DNS の変更は不要です
- バックエンド（GAS 受付窓口）側は対応済みで、**先に配置しても問題ありません**

## 現在配信中の版

配信中のファイルはコミット `295041d`（2026-07-31）の内容と一致していました。
今回は **13コミット**がまとめて反映されます。

## 反映される内容

### 新機能

| 内容 | 説明 |
|---|---|
| ログアウト／アカウント切り替え | サイドバー下部にログイン中のアカウント表示と2つのボタンを追加。これまでログアウト手段が無かった |
| ログイン画面の改善 | 未ログイン時に自動でログイン画面を表示。接続先URLの入力が不要になり、ID・パスワードだけで入れる。パスワードの表示切り替え（目のアイコン）と、送信中の「ログイン中…」表示を追加 |
| ロードマップ画面の刷新 | 横向きの進捗ゲージ（完了ぶんがグラデーションで塗られる）、フェーズ別アコーディオン、フェーズ冒頭の解説動画リンク |
| クラス詳細からDiscordへ | クラス詳細モーダルの右上に「クラスチャンネルを開く」ボタンを追加 |

### 不具合修正

| 内容 | 影響範囲 |
|---|---|
| 読み込み完了時にスクロール位置が先頭へ戻る | **全画面**。初回読み込み中にスクロールしていると先頭へ飛ばされていた |
| 講師マスタの「クラスの講義出席状況を編集」が無反応 | 講師マスタ。ヘッダーの講師フィルタが別の講師だとボタンが効かなかった |
| ロードマップのチェックが即座に反映されない | ロードマップ。押しても数秒間チェックが付かなかった |
| 講師未選択時にチェックボックスが無反応に見える | ロードマップ。理由の表示と、その場で講師を選べるUIを追加 |

## 反映後の確認方法

1. `https://app.levela.co.jp/instructor-dashboard/` をブラウザで開く（**強制リロード**: Mac は Cmd+Shift+R）
2. **サイドバーの一番下**にログイン中のアカウント名と「アカウント切り替え」「ログアウト」が出ている
3. **ロードマップ**を開くと、上部に横向きの進捗ゲージ（丸い番号が並んだ矢印）が出ている
4. **クラス一覧**からクラスを開くと、右上に紫の「クラスチャンネルを開く」ボタンが出ている（一部の旧期クラスでは出ません。仕様です）

## 切り戻し

上記バックアップを書き戻すだけで元に戻ります。データベースやスプレッドシートには一切影響しません。

## 連絡先

不明点があれば 山下優花（CS/CTO室）まで。
